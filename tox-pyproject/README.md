# Tox Pyproject

Example project.

Running the following works.

```shell
pip install .
pip install .[test]
pytest tests/
tox
```

There are problems with tox in the related tox-pyproject-plugins package.
